#include <iostream>
#include <string>
using namespace std;

int main() {
    string search;
    cout << "nu gusto mo isearch? (CAPS AH!) :";
    cin >> search;
    
    if (search == "ALGORITHM") {
        cout << "a set of instructions to solve a problem";
    }
   else if (search == "BLUEPRINT") {
    cout << "a detailed plan or diagram";
	}
	else if (search == "CIRCUIT") {
    cout << "a path through which electric current flows";
	}
	else if (search == "DATABASE") {
    cout << "a collection of organized data";
	}
	else if (search == "EFFICIENCY") {
    cout << "the ratio of output to input";
	}
	else if (search == "FEEDBACK") {
    cout << "information about a system's performance";
	}
	else if (search == "GENERATOR") {
    cout << "a machine that converts energy 	into electricity";
	}
	else if (search == "HARDWARE") {
    cout << "physical components of a 		computer system";
	}
	else if (search == "INTERFACE") {
    cout << "a connection between two systems";
	}
	else if (search == "JUNCTION") {
    cout << "a point where two or more conductors meet";
	}
	else if (search == "KINEMATICS") {
    cout << "the study of motion";
	}
	else if (search == "LOAD") {
    cout << "a force or weight applied to a system";
	}
	else if (search == "MICROCHIP") {
    cout << "a small electronic device";
	}
	else if (search == "NETWORK") {
    cout << "a collection of interconnected devices";
	}
	else if (search == "OPTIMIZATION") {
    cout << "the process of improving a system's performance";
	}
	else if (search == "PISTON") {
    cout << "a moving part in an engine";
	}
	else if (search == "QUEUE") {
    cout << "a line of items waiting to be processed";
	}
	else if (search == "ROBOTICS") {
    cout << "the study of robots and their applications";
	}
	else if (search == "SENSOR") {
    cout << "a device that detects and measures physical parameters";
	}
	else if (search == "SIMULATION") {
    cout << "a model of a real-world system";
	}
	else if (search == "SOFTWARE") {
    cout << "programs and operating systems";
	}
	else if (search == "TURBINE") {
    cout << "a machine that converts energy into rotational motion";
	}
	else if (search == "VALVE") {
    cout << "a device that controls fluid flow";
	}
	else if (search == "WATT") {
    cout << "a unit of electrical power";
	}
	else if (search == "X-AXIS") {
    cout << "a coordinate axis in a graphical representation";
	}
	else if (search == "YIELD") {
    cout << "the amount of product obtained from a process";
	}
	else if (search == "ACTUATOR") {
    cout << "a device that converts energy into motion";
	}
	else if (search == "BEARING") {
    cout << "a device that supports moving parts";
	}
	else if (search == "CATALYST") {
    cout << "a substance that speeds up a chemical reaction";
	}
	else if (search == "WATER") {
    cout << "a clear, colorless liquid";
	}

    else {
        cout << "wala ‘yan dito";
    }

    return 0;
}











